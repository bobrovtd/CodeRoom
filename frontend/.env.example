# Production uses same-origin /api and WebSocket URLs by default.
# Uncomment these values only when the frontend must connect directly to a
# separately hosted backend during local development.
# VITE_API_BASE_URL=http://localhost:4000/api
# VITE_WS_BASE_URL=ws://localhost:4000
